document.addEventListener('DOMContentLoaded', () => {
    const jsConfetti = new JSConfetti()

    jsConfetti.addConfetti({
        emojis: ['🌊','🏄🏻‍♂️','🐚','🏖️','🎣'],
        emojiSize: 30,
        confettiNumber: 300,
    })
}) 